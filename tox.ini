[tox]
envlist = py35,py36,qa
skip_missing_interpreters = True

[testenv]
commands =
    python setup.py install
    coverage run --include=ledshimdemo/*,tests/* -m unittest -v
    coverage report
deps =
    coverage
    mock

[testenv:qa]
commands =
    # Don't check if long string meta-data syntax are reStructuredText-compliant (-r option)
    python setup.py check -ms
    flake8 --ignore E501,E221,F401,E722 ledshimdemo
    flake8 --ignore E501,E402 tests
deps =
    docutils
    flake8
